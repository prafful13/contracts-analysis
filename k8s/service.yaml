apiVersion: v1
kind: Service
metadata:
  name: contracts-analysis
  namespace: contracts-analysis
spec:
  type: NodePort
  selector:
    app: contracts-analysis
  ports:
    - port: 8501
      targetPort: 8501
      nodePort: 30502
      name: streamlit
